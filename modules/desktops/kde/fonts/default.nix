{
  # System Settings -> Text & Fonts
  programs.plasma = {
    /*
      fonts = {
        general = {
          family = "Noto Sans";
          pointSize = 10;
        };
        fixedWidth = {
          family = "Hack";
          pointSize = 13;
        };
        small = {
          family = "Noto Sans";
          pointSize = 8;
        };
        toolbar = { };
        menu = { };
        windowTitle = { };
      };

      configFile.kdeglobals.General = {
        # Anti-Aliasing
        XftAntialias = true;

        # Sub-pixel rendering
        XftSubPixel = "rgb";

        # Hinting
        XftHintStyle = "hintslight";
      };
    */
  };
}
