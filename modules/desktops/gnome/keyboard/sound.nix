{
  # Settings -> Keyboard -> Keyboard Shortcuts -> Sound and Media
  dconf.settings = {
    "org/gnome/settings-daemon/plugins/media-keys" = {
      #eject = [ ];
      #media = [ ];
      #mic-mute = [ ];
      #next = [ ];
      #pause = [ ];
      #play = [ ];
      #previous = [ ];
      #stop = [ ];
      #volume-down = [ ];
      #volume-mute = [ ];
      #volume-up = [ ];
    };
  };
}
